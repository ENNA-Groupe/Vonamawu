import { ByCategoriePipe } from './by-categorie.pipe';

describe('ByCategoriePipe', () => {
  it('create an instance', () => {
    const pipe = new ByCategoriePipe();
    expect(pipe).toBeTruthy();
  });
});
