import { SearchEntityPipe } from './search-entity.pipe';

describe('SearchEntityPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEntityPipe();
    expect(pipe).toBeTruthy();
  });
});
