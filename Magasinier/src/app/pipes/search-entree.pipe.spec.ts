import { SearchEntreePipe } from './search-entree.pipe';

describe('SearchEntreePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEntreePipe();
    expect(pipe).toBeTruthy();
  });
});
