import { WorstSellPipe } from './worst-sell.pipe';

describe('WorstSellPipe', () => {
  it('create an instance', () => {
    const pipe = new WorstSellPipe();
    expect(pipe).toBeTruthy();
  });
});
