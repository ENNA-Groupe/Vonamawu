import { SearchOperationPipe } from './search-operation.pipe';

describe('SearchOperationPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchOperationPipe();
    expect(pipe).toBeTruthy();
  });
});
