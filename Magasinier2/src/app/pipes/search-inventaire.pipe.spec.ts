import { SearchInventairePipe } from './search-inventaire.pipe';

describe('SearchInventairePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchInventairePipe();
    expect(pipe).toBeTruthy();
  });
});
