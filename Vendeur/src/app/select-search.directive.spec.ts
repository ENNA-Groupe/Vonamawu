import { SelectSearchDirective } from './select-search.directive';

describe('SelectSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectSearchDirective();
    expect(directive).toBeTruthy();
  });
});
